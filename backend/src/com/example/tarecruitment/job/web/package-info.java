/**
 * Job domain HTTP entry layer.
 *
 * <p>JobServlet only parses HTTP path/parameters, reads current user, calls JobService, and writes unified JSON.
 */
package com.example.tarecruitment.job.web;
